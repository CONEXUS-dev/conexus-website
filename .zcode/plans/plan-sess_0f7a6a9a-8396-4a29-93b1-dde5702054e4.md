# CONEXUS 2.0 — 12-Column Grid Anchoring Refactor

## Finding: Vault metric already correct
Both the current `content/vault.ts` and `CONEXUS Website 2.0 Substance.md` read **361.8%** in all three locations. The directive's override is already satisfied — zero vault changes.

## Finding: Referenced `_7` and `_2` files do not exist
Only `Substance.md` and `Prompt Creation.md` are present. No new spec to extract from.

---

## Refactor Plan — 27 edits across 10 files

### Rule set applied everywhere
- Every major section block wrapped in `grid grid-cols-12 gap-8` (or `gap-4` for tighter sub-grid)
- Massive typography constrained: `col-span-12 md:col-span-8` for serifs, `md:col-span-4` for supporting text
- Images locked to exact column fractions (never full-width float)
- Vertical spacing: `py-12` (compact) or `py-24` (standard) — no `py-32` or `mt-32` anywhere
- Hero CTA and data labels snapped to the bottom of the `100vh` sticky viewport

---

### 1. `components/primitives.tsx` — SectionShell vertical rhythm
- Change outer padding from `py-24 md:py-32` → `py-12 md:py-24`

### 2. `components/hero/ScrollRunway.tsx` — Snap CTA to viewport bottom
- Tighten hero inner flex to `justify-between` (already set, but the bottom-row needs explicit `items-end` alignment verified)
- Reduce inner padding top from `pt-24 md:pt-28` → `pt-20 md:pt-24` so content doesn't float above the fold

### 3. `sections/Validation.tsx` — Grid lock the condition table
- Wrap the four-arm condition table in an explicit `grid grid-cols-12 gap-8`
- Constrain the condition list to `md:col-span-7` and the stat cells to `md:col-span-5` with an explicit parent grid
- Reduce `mt-16` → `mt-12` on the condition grid

### 4. `sections/Products.tsx` — Constrain the gateway instruction text
- The instruction text ("SELECT A GATEWAY…") is already in `md:col-span-4` but the parent grid `gap-8` is fine — no changes needed here

### 5. `components/products/ProductGateways.tsx` — Lock cards to grid fractions
- Cards are already in a `grid grid-cols-12` — compliant. No changes.

### 6. `sections/Team.tsx` — Lock portrait to exact column fraction
- Portrait image container: constrain to `col-span-12 md:col-span-4` (already set) — verify parent grid exists (it does)
- Reduce `mt-8` on quote → `mt-6` for tighter rhythm

### 7. `sections/Inversion.tsx` — Reduce vertical gaps
- Change `mt-16` → `mt-12` on the stats grid
- Change `mt-16` → `mt-12` on the baselines/forgetting grid
- Change `mt-16` → `mt-12` on the "Why" block

### 8. `sections/Manifesto.tsx` — Vertical rhythm + image anchoring
- Act vertical padding: `py-20 md:py-28` → `py-12 md:py-16` (tighter, no cinematic dissolve)
- Image grid: already in `grid grid-cols-12 gap-6` with `md:col-span-6` — compliant
- Reduce `mt-10` → `mt-8` on the four pillars grid

### 9. `sections/Evidence.tsx` — Three `mt-32` blocks → `mt-24`
- Line 140: Primary causal study inner block `mt-24` → keep (already correct after refactor)
- Line 170: Forgetting Engine `mt-32` → `mt-24`
- Line 204: Complexity Inversion `mt-32` → `mt-24`
- KOI case study `mt-32` → `mt-24`
- Image pairs: already in `grid grid-cols-12 gap-6` with `md:col-span-6` — compliant
- Benchmark cards: already in `grid-cols-12` — compliant
- Reduce `mt-20` on supported/limits → `mt-12`

### 10. `sections/Nairthex.tsx` — Vertical rhythm
- Anamorphic image `mt-16` → `mt-12`
- Doctrine `mt-24` → `mt-16`
- Clarity `mt-24` → `mt-16`
- One quiet conversation `mt-24` → `mt-16`
- CTA `mt-24` → `mt-16`
- CTA inner `pt-16` → `pt-12`

### 11. `sections/Echoform.tsx` — Vertical rhythm
- Experience `mt-24` → `mt-16`
- Clarity `mt-24` → `mt-16`
- Screens `mt-24` → `mt-12`

### 12. `sections/Footer.tsx` — Already compliant
- Grid-cols-12 with proper fractions — no changes needed.

---

## Client/Server boundary: NO CHANGES
All edits are Tailwind class-string changes within existing RSC and Client component files. No files cross the `"use client"` boundary. No hydration risk.

## Post-refactor verification
- `tsc --noEmit` type gate
- `npm run build` production build gate
- Both must pass clean before declaring complete.